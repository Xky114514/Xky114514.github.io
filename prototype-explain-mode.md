# 前端原型“业务说明模式”使用与维护规范

## 目标

原型需要同时服务两种场景：

1. **正常演示**：默认关闭说明模式，界面外观和原有交互不变。
2. **原型评审**：点击页面右上角“业务说明”进入说明模式，再点选页面上的字段、表头、按钮或选择项，在右侧查看该页面元素的业务含义、当前状态、数据来源、交互结果和异常反馈。

说明模式不是线上产品功能，也不代替 PRD、流程文档或接口文档。它只解决“看到一个页面元素时，能够立刻读到这个元素的原型文字说明”。业务背景、跨页流程、研发实现和迭代记录不在【业务说明】中展示。

## 页面上的标记

- 蓝框：已从当前页面批注中找到对应定义。
- 橙框：原型上存在该控件，但批注中还没有定义，需要业务或产品补充。
- 深蓝实线框：当前正在查看的控件。
- 说明模式中点击控件只查看定义，不执行原交互；点击“退出说明模式”后恢复正常演示。

## 一条合格说明应包含什么

业务和产品维护说明时，统一使用下列句式：

```text
字段【供应商】表示实际送货供应商；由 AI 初步识别，操作员可修改；修改后重新筛选可关联采购单。
按钮【确认提交】校验供应商、商品和实收数；校验通过后提交观麦，失败时保留当前草稿并展示原因。
按钮【补充到此单】仅在候选单可继续补单时可用；点击后二次确认，成功后回到当前详情并更新提交状态。
```

方括号中的名称必须与界面文案一致。相同名称可以写多条，系统会合并展示；不直接对应页面元素的背景、目标、流程结论或研发备注不得写入该列表。

每条说明按实际适用范围回答以下问题：

1. **是什么**：字段或操作的业务含义。
2. **从哪里来**：人工输入、AI 识别、观麦同步、关联单据还是系统计算。
3. **影响什么**：筛选、计算、关联、保存、提交、审核或状态同步。
4. **怎么交互**：何时显示、何时可用、点击或输入后页面如何变化。
5. **异常怎么办**：是否阻断、显示什么提示、当前内容是否保留、能否重试或返回。

## 原型说明与接口契约的边界

说明模式会从当前 DOM 读取控件类型、示例值、必填标记和可编辑状态，但不会猜测接口字段名。接口字段名、精确数据类型、枚举编码、长度、精度和空值规则必须由前后端在接口文档中共同确认。

推荐接口文档对每个字段补齐：

| 项目 | 示例 |
| --- | --- |
| 业务名称 | 供应商 |
| 接口字段 | `supplier_id` |
| 数据类型 | `string` |
| 是否必填 | 是 |
| 数据来源 | 供应商主数据 / AI 候选 / 人工选择 |
| 可编辑条件 | 待提交可编辑，已提交只读 |
| 校验规则 | 必须存在且未停用；与关联采购单供应商一致 |
| 上游依赖 | 原始消息、供应商主数据 |
| 下游影响 | 采购单候选、补单候选、提交校验 |
| 异常处理 | 无匹配时允许人工输入并进入待治理名单 |

## 说明范围

- **纳入【业务说明】**：当前页面可见的字段、表头、按钮、链接、页签、开关、单选项、下拉项，以及打开弹窗后实际出现的元素。
- **不纳入【业务说明】**：业务背景、目标、非目标、角色权限总表、跨页流程、状态机总表、接口字段、技术实现、验收用例和迭代记录。
- **呈现规则**：目录只展示当前视图实际可见的元素；切换页签、打开弹窗或进入不同单据状态后，目录随页面元素同步更新。

评审结束的最低标准：当前视图不遗留无法解释的橙框元素；关键提交链路的字段和按钮能说明数据来源、可用条件、校验、反馈与失败保留规则。

## 数据维护方式

页面元素说明继续维护在 `annotations.json` 的 `pageAnnotations[*].business` 中。原型内的“编辑说明”可先保存本地草稿，再通过“保存 JSON”覆盖项目文件。说明模式会自动解析带有 `字段【】`、`输入【】`、`按钮【】`、`弹窗【】`、`页签【】`、`开关【】`、`单选【】` 或 `下拉【】` 前缀的说明，并只呈现当前视图中能够匹配到的页面元素。
