# 群成员绑定审核员

## 需求范围

在「销售订单录单 → 群聊管理 → 群聊详情 → 群成员」列表中，于“绑定客户”右侧新增“绑定审核员”字段。

## 功能规则

- 字段采用单选下拉，同一群成员最多只能绑定一名审核员。
- 下拉不支持多选或全选，切换选项后以最后一次选择的审核员为准。
- 选项包含当前租户全部订单审核员及管理员。
- 不选择审核员时，默认沿用群聊配置的审核员。
- 成员产生订单后，优先使用该成员绑定的审核员。
- 审核员优先级：群成员绑定审核员 > 群聊审核员 > 系统默认规则。
- 配置按群成员独立保存，互不影响。
- 邮箱类型不展示该字段。
