# 录单入口与系统管理导航调整需求说明

## 1. 文档信息

- 版本：V1.2
- 日期：2026-08-07
- 状态：界面已实现；角色默认首页与菜单权限待账号角色数据接入
- 变更范围：默认首页规则、销售录单壳层、采购录单壳层、销售回单子模块壳层、系统管理入口

## 2. 背景与目标

- [事实] 原平台存在独立“首页/应用中心”，用户进入后需要再次选择销售录单或采购录单。
- [事实] 成员管理、额度管理已有独立页面，但销售录单和采购录单内缺少统一、直接的管理员入口。
- [事实] 页面左上角需要明确展示当前应用名称，避免用户无法判断当前处于销售录单还是采购录单。
- [事实] 底部应用切换器需要保留，但只用于销售录单与采购录单互相切换，不再显示“首页”。
- [事实] 业务应用顶部需要提供“返回 PRD 首页”按钮，当前应用名称需要使用高亮色识别。
- [事实] 系统管理员默认进入销售录单；普通操作员按角色进入对应应用，同时具备销售和采购角色时仍默认进入销售录单。
- [事实] “系统管理员”菜单仅对系统管理员显示，普通操作员不显示该入口。

本次目标：缩短首次进入路径，统一系统管理入口，并在保留业务应用互切能力的同时，使当前应用身份和 PRD 返回路径清晰可见。

## 3. 范围与非目标

### 3.1 本次范围

1. 移除独立首页/应用中心的可访问界面。
2. 用户首次进入站点时，根据启用角色进入销售录单或采购录单首页。
3. 兼容旧 `#home` 地址，并将其重定向到销售录单首页。
4. 销售录单、采购录单及销售回单子模块左上角展示企业名称和对应应用名称。
5. “系统管理员”改为可展开菜单，包含“成员管理”和“额度管理”。
6. 点击管理员菜单项后进入现有成员管理或额度管理页面。
7. 保留底部应用快捷切换器，移除其中的“首页”入口，仅提供销售录单与采购录单之间的互切入口。
8. 在业务应用顶部新增“返回 PRD 首页”按钮，并高亮显示当前应用名称。
9. 根据登录账号角色确定默认业务首页；系统管理员和多角色操作员统一优先进入销售录单。
10. 根据管理员身份控制“系统管理员”菜单可见性。

### 3.2 非目标

1. 不调整成员管理、额度管理页面内部字段、数据和业务规则。
2. 不调整销售录单、采购录单和销售回单的业务处理流程。
3. 不恢复独立应用中心，也不在快捷切换器中展示“首页”入口。
4. 不调整登录、退出登录及角色授权流程；前端默认首页和菜单展示需读取登录态提供的角色结果。

## 4. 角色与数据范围

| 角色 | 页面入口 | 可执行操作 | 数据范围 |
|---|---|---|---|
| 销售操作员/回单操作员 | 登录后进入销售录单首页 | 使用已授权的销售录单、销售回单功能 | 当前租户、当前账号有权访问的数据 |
| 采购操作员 | 仅具备采购角色时进入采购录单首页 | 使用已授权的采购录单功能 | 当前租户、当前账号有权访问的数据 |
| 销售＋采购多角色操作员 | 登录后优先进入销售录单首页 | 使用两个角色已授权的业务功能 | 两个角色权限范围的并集 |
| 系统管理员 | 登录后进入销售录单首页；顶部显示“系统管理员”菜单 | 进入成员管理、额度管理 | 当前租户的成员与额度数据 |

> [实现约束] 当前静态原型没有真实登录态，暂固定展示系统管理员场景；正式实现必须以登录态返回的角色和管理员标识决定默认首页及菜单可见性。

## 5. 页面入口与默认状态

| 场景 | 入口 | 结果 | 默认状态 |
|---|---|---|---|
| 系统管理员首次进入 | 登录后访问根地址 | 跳转至 `/ai_order/home.html` | 销售录单首页 |
| 销售/回单操作员首次进入 | 登录后访问根地址 | 跳转至 `/ai_order/home.html` | 销售录单首页 |
| 仅采购操作员首次进入 | 登录后访问根地址 | 打开 `/index.html#purchase-home` | 采购录单首页 |
| 销售＋采购多角色操作员首次进入 | 登录后访问根地址 | 跳转至 `/ai_order/home.html` | 销售录单首页 |
| 旧首页兼容 | `/index.html#home` | 跳转至 `/ai_order/home.html` | 销售录单首页 |
| 采购录单直达 | `/index.html#purchase-home` | 打开采购录单首页 | 采购首页菜单选中 |
| 销售切换采购 | 销售录单左下角“切换应用” → 采购录单 | 打开 `/index.html#purchase-home` | 采购首页菜单选中 |
| 采购切换销售 | 采购录单左下角“切换应用” → 销售录单 | 打开 `/ai_order/home.html` | 销售录单首页 |
| 返回 PRD 首页 | 业务应用右上角“返回 PRD 首页” | 打开 `/index.html#projects` | PRD 项目库 |
| 成员管理 | 系统管理员 → 成员管理 | 打开 `/index.html#tenant-members` | 成员管理菜单选中 |
| 额度管理 | 系统管理员 → 额度管理 | 打开 `/index.html#tenant-quota` | 额度管理菜单选中 |

## 6. 页面展示与交互规则

### BR-01 首次进入

- 当地址不包含业务路由时，系统读取当前账号的启用角色。
- 系统管理员、销售操作员、回单操作员进入销售录单首页。
- 仅具备采购角色的操作员进入采购录单首页。
- 同时具备销售域角色和采购角色时，销售录单优先，进入销售录单首页。
- 角色接口失败或未返回有效角色时，按安全降级进入销售录单首页，但隐藏“系统管理员”菜单。
- 用户不再看到独立首页或应用中心。
- 旧 `#home` 地址执行相同跳转，避免历史收藏地址进入空白页或失效页。

### BR-02 应用名称

- 销售录单页面左上角显示“测试企业 1318｜销售录单”。
- 采购录单页面左上角显示“测试企业 1318｜采购录单”。
- 销售回单作为销售录单子模块，左上角仍显示“测试企业 1318｜销售录单”。
- “销售录单”或“采购录单”使用主题蓝色、高字重展示，企业名称保持正文色。
- 应用名称仅用于识别当前业务域，不改变页面标题、菜单和数据范围。

### BR-03 系统管理员菜单

- 仅当登录账号具有系统管理员身份时显示“系统管理员”菜单；普通操作员不展示该菜单，不采用置灰态。
- 成员管理、额度管理路由同时校验系统管理员权限；普通操作员通过直达地址访问时拒绝进入，避免仅隐藏菜单但仍可越权访问。
- 点击“系统管理员”后展开下拉菜单。
- 菜单项按“成员管理、额度管理”顺序展示。
- 点击“成员管理”进入成员管理页面；点击“额度管理”进入额度管理页面。
- 菜单展开不修改业务数据，不弹出二次确认。
- 页面跳转失败时保留当前页面；正式产品应使用统一错误反馈，本次静态原型不新增网络错误态。

### BR-04 应用快捷切换

- 销售录单、采购录单及销售回单子模块左下角显示快捷切换图标；展开后按“销售录单、采购录单”顺序显示两个应用。
- 当前所在应用标记为“当前应用”并使用浅色背景高亮；另一个应用显示“进入销售录单”或“进入采购录单”。
- 快捷切换弹层不显示“首页”或“录单平台首页”。
- 点击弹层外区域或按 `Esc` 键时关闭弹层；该操作不修改业务数据。
- 销售录单和销售回单子模块不展示“返回应用中心”。
- 系统管理页面侧边栏仅保留“成员管理”和“额度管理”，不展示“首页”。

### BR-05 返回 PRD 首页

- 销售录单、采购录单及销售回单子模块右上角显示“返回 PRD 首页”。
- 点击后打开 `/index.html#projects`，进入 PRD 项目库。
- 该按钮与“业务说明”“系统管理员”并列，不修改当前业务数据。
- 当前页面存在未保存数据时，本次静态原型不新增离开确认；正式产品应复用业务页面已有的未保存离开保护。

## 7. 边界与异常

| 场景 | 系统处理 | 用户结果 |
|---|---|---|
| 访问旧 `#home` | 重定向至销售录单 | 正常进入销售录单，不展示旧首页 |
| 系统管理员直接访问成员/额度路由 | 校验管理员身份后按目标路由渲染 | 可直接查看对应管理页面 |
| 普通操作员直接访问成员/额度路由 | 拒绝进入系统管理页面 | 显示无权限反馈并留在当前业务应用 |
| 在销售与采购之间切换 | 使用左下角快捷切换器 | 直接进入目标应用首页，弹层中不显示“首页” |
| 从业务应用返回 PRD | 点击右上角“返回 PRD 首页” | 进入 PRD 项目库 |
| 从管理页面返回业务应用 | 暂不新增固定返回按钮 | 使用浏览器返回或重新进入目标应用 |
| 无 JavaScript | 自动跳转和管理员菜单增强不可用 | 不属于本次静态原型保障范围 |
| 同时具备销售和采购角色 | 按销售优先级处理 | 进入销售录单首页，仍可通过左下角切换至采购录单 |
| 角色数据获取失败或为空 | 进入销售录单并隐藏管理员菜单 | 用户可进入默认业务页，不暴露管理入口 |

## 8. 验收标准

### AC-ENTRY-01 管理员默认进入销售录单

- Given 系统管理员访问站点根地址且未指定路由。
- When 页面加载完成。
- Then 地址跳转至 `/ai_order/home.html`。
- And 页面标题为“首页｜销售录单”。

### AC-ENTRY-02 采购操作员默认进入采购录单

- Given 操作员仅具备采购角色。
- When 登录后访问站点根地址。
- Then 地址进入 `/index.html#purchase-home`。
- And 页面左上角显示“采购录单”。

### AC-ENTRY-03 多角色操作员优先进入销售录单

- Given 操作员同时具备销售域角色和采购角色。
- When 登录后访问站点根地址。
- Then 地址进入 `/ai_order/home.html`。
- And 不经过原应用中心首页。

### AC-ENTRY-04 旧首页兼容

- Given 用户访问 `/index.html#home`。
- When 页面加载完成。
- Then 系统进入销售录单首页。
- And 不展示整体额度、应用中心和应用卡片组成的旧首页。

### AC-HEADER-01 销售录单名称

- Given 用户位于销售录单任一页面。
- When 查看页面左上角。
- Then 显示“测试企业 1318”和“销售录单”。
- And “销售录单”使用主题蓝色高亮展示。

### AC-HEADER-02 采购录单名称

- Given 用户位于采购录单任一页面。
- When 查看页面左上角。
- Then 显示“测试企业 1318”和“采购录单”。
- And “采购录单”使用主题蓝色高亮展示。

### AC-ADMIN-01 展开管理员菜单

- Given 当前页面显示“系统管理员”。
- When 用户点击“系统管理员”。
- Then 展开菜单。
- And 菜单仅显示“成员管理”和“额度管理”。

### AC-ADMIN-02 普通操作员不显示管理员菜单

- Given 当前账号不具有系统管理员身份。
- When 销售录单或采购录单页面加载完成。
- Then 页面不显示“系统管理员”菜单。
- And 用户通过直达地址访问成员管理或额度管理时收到无权限反馈，不渲染管理数据。

### AC-ADMIN-03 进入成员管理

- Given 管理员菜单已展开。
- When 用户点击“成员管理”。
- Then 地址变为 `/index.html#tenant-members`。
- And 页面主标题显示“成员管理”。

### AC-ADMIN-04 进入额度管理

- Given 管理员菜单已展开或用户位于系统管理侧边栏。
- When 用户点击“额度管理”。
- Then 地址变为 `/index.html#tenant-quota`。
- And 页面主标题显示“额度管理”。

### AC-NAV-01 销售录单切换采购录单

- Given 用户位于销售录单或销售回单页面。
- When 用户展开左下角快捷切换器。
- Then 弹层按顺序显示“销售录单”和“采购录单”。
- And “销售录单”标记为“当前应用”，“采购录单”可点击进入。
- And 不显示“首页”“录单平台首页”或“返回应用中心”。

### AC-NAV-02 采购录单切换销售录单

- Given 用户位于采购录单页面。
- When 用户展开左下角快捷切换器。
- Then 弹层按顺序显示“销售录单”和“采购录单”。
- And “采购录单”标记为“当前应用”。
- And 点击“销售录单”后进入 `/ai_order/home.html`。

### AC-NAV-03 返回 PRD 首页

- Given 用户位于销售录单、采购录单或销售回单页面。
- When 用户点击右上角“返回 PRD 首页”。
- Then 地址变为 `/index.html#projects`。
- And 页面显示 PRD 项目库。

## 9. 待确认与后续建议

| 级别 | 问题 | 当前实现/推荐默认值 | 影响 |
|---|---|---|---|
| 可带假设推进 | 从成员/额度页面返回业务应用的方式 | 当前使用浏览器返回或外部系统入口 | 若要求站内闭环，应补充返回目标和记忆来源页规则 |
| 可带假设推进 | 企业名称是否固定为“测试企业 1318” | 当前按原型截图固定展示 | 接入真实租户后应读取当前租户名称 |
| 开发前置依赖 | 登录态角色字段及管理员标识的数据来源 | 复用现有账号权限接口返回的启用角色集合与管理员标识 | 未接入前无法按角色跳转或隐藏管理员菜单 |

## 10. 一致性审查结论

- 术语统一使用“销售录单”“采购录单”“成员管理”“额度管理”。
- 旧“AI 销售订单应用”“应用中心”“录单平台首页”不再作为当前入口文案。
- 默认入口、角色优先级、管理员菜单权限、旧地址兼容、管理页面、应用名称、快捷切换和 PRD 返回入口均有对应验收标准。
- V1.2 明确了系统管理员、单角色操作员和多角色操作员的默认首页，不再使用“所有用户默认进入销售录单”的笼统规则。
- 界面交互不存在 P0 冲突；角色路由和管理员菜单权限依赖登录态提供角色数据，接入前不可标记为完整验收通过。
