# 录单入口与系统管理导航调整需求说明

## 1. 文档信息

- 版本：V1.0
- 日期：2026-08-07
- 状态：已实现、待业务验收
- 变更范围：平台首页、销售录单壳层、采购录单壳层、销售回单子模块壳层、系统管理入口

## 2. 背景与目标

- [事实] 原平台存在独立“首页/应用中心”，用户进入后需要再次选择销售录单或采购录单。
- [事实] 成员管理、额度管理已有独立页面，但销售录单和采购录单内缺少统一、直接的管理员入口。
- [事实] 页面左上角需要明确展示当前应用名称，避免用户无法判断当前处于销售录单还是采购录单。
- [事实] 原底部应用切换器包含首页、销售录单、采购录单入口，本次需要移除。

本次目标：缩短首次进入路径，统一系统管理入口，并使当前应用身份清晰可见。

## 3. 范围与非目标

### 3.1 本次范围

1. 移除独立首页/应用中心的可访问界面。
2. 用户首次进入站点时直接进入销售录单首页。
3. 兼容旧 `#home` 地址，并将其重定向到销售录单首页。
4. 销售录单、采购录单及销售回单子模块左上角展示企业名称和对应应用名称。
5. “系统管理员”改为可展开菜单，包含“成员管理”和“额度管理”。
6. 点击管理员菜单项后进入现有成员管理或额度管理页面。
7. 移除底部应用切换器和“返回应用中心”入口。

### 3.2 非目标

1. 不调整成员管理、额度管理页面内部字段、数据和业务规则。
2. 不调整销售录单、采购录单和销售回单的业务处理流程。
3. 不新增销售录单与采购录单之间的站内切换入口。
4. 不调整登录、退出登录、账号鉴权或后端权限接口。

## 4. 角色与数据范围

| 角色 | 页面入口 | 可执行操作 | 数据范围 |
|---|---|---|---|
| 普通业务用户 | 首次进入销售录单；通过既有直达地址进入授权应用 | 使用销售录单或采购录单业务功能 | 当前租户、当前账号有权访问的数据 |
| 系统管理员 | 销售录单或采购录单顶部“系统管理员”菜单 | 进入成员管理、额度管理 | 当前租户的成员与额度数据 |

> [推断] 当前静态原型固定展示“系统管理员”。生产环境应由账号权限决定菜单是否可见；本次不新增鉴权实现。

## 5. 页面入口与默认状态

| 场景 | 入口 | 结果 | 默认状态 |
|---|---|---|---|
| 首次进入 | 根地址 `/` 或 `/index.html` | 跳转至 `/ai_order/home.html` | 销售录单首页 |
| 旧首页兼容 | `/index.html#home` | 跳转至 `/ai_order/home.html` | 销售录单首页 |
| 采购录单直达 | `/index.html#purchase-home` | 打开采购录单首页 | 采购首页菜单选中 |
| 成员管理 | 系统管理员 → 成员管理 | 打开 `/index.html#tenant-members` | 成员管理菜单选中 |
| 额度管理 | 系统管理员 → 额度管理 | 打开 `/index.html#tenant-quota` | 额度管理菜单选中 |

## 6. 页面展示与交互规则

### BR-01 首次进入

- 当地址不包含业务路由时，系统自动进入销售录单。
- 用户不再看到独立首页或应用中心。
- 旧 `#home` 地址执行相同跳转，避免历史收藏地址进入空白页或失效页。

### BR-02 应用名称

- 销售录单页面左上角显示“测试企业 1318｜销售录单”。
- 采购录单页面左上角显示“测试企业 1318｜采购录单”。
- 销售回单作为销售录单子模块，左上角仍显示“测试企业 1318｜销售录单”。
- 应用名称仅用于识别当前业务域，不改变页面标题、菜单和数据范围。

### BR-03 系统管理员菜单

- 点击“系统管理员”后展开下拉菜单。
- 菜单项按“成员管理、额度管理”顺序展示。
- 点击“成员管理”进入成员管理页面；点击“额度管理”进入额度管理页面。
- 菜单展开不修改业务数据，不弹出二次确认。
- 页面跳转失败时保留当前页面；正式产品应使用统一错误反馈，本次静态原型不新增网络错误态。

### BR-04 入口移除

- 销售录单、采购录单和销售回单子模块不展示底部应用切换器。
- 销售录单和销售回单子模块不展示“返回应用中心”。
- 系统管理页面侧边栏仅保留“成员管理”和“额度管理”，不展示“首页”。

## 7. 边界与异常

| 场景 | 系统处理 | 用户结果 |
|---|---|---|
| 访问旧 `#home` | 重定向至销售录单 | 正常进入销售录单，不展示旧首页 |
| 直接访问成员/额度路由 | 按目标路由渲染页面 | 可直接查看对应管理页面 |
| 移除首页后进入采购录单 | 保留原采购直达地址 | 通过外部入口或直达地址进入 |
| 从管理页面返回业务应用 | 本次不新增返回按钮 | 使用浏览器返回或外部系统入口 |
| 无 JavaScript | 自动跳转和管理员菜单增强不可用 | 不属于本次静态原型保障范围 |

## 8. 验收标准

### AC-ENTRY-01 默认进入销售录单

- Given 用户访问站点根地址且未指定路由。
- When 页面加载完成。
- Then 地址跳转至 `/ai_order/home.html`。
- And 页面标题为“首页｜销售录单”。

### AC-ENTRY-02 旧首页兼容

- Given 用户访问 `/index.html#home`。
- When 页面加载完成。
- Then 系统进入销售录单首页。
- And 不展示整体额度、应用中心和应用卡片组成的旧首页。

### AC-HEADER-01 销售录单名称

- Given 用户位于销售录单任一页面。
- When 查看页面左上角。
- Then 显示“测试企业 1318”和“销售录单”。

### AC-HEADER-02 采购录单名称

- Given 用户位于采购录单任一页面。
- When 查看页面左上角。
- Then 显示“测试企业 1318”和“采购录单”。

### AC-ADMIN-01 展开管理员菜单

- Given 当前页面显示“系统管理员”。
- When 用户点击“系统管理员”。
- Then 展开菜单。
- And 菜单仅显示“成员管理”和“额度管理”。

### AC-ADMIN-02 进入成员管理

- Given 管理员菜单已展开。
- When 用户点击“成员管理”。
- Then 地址变为 `/index.html#tenant-members`。
- And 页面主标题显示“成员管理”。

### AC-ADMIN-03 进入额度管理

- Given 管理员菜单已展开或用户位于系统管理侧边栏。
- When 用户点击“额度管理”。
- Then 地址变为 `/index.html#tenant-quota`。
- And 页面主标题显示“额度管理”。

### AC-NAV-01 移除旧入口

- Given 用户位于销售录单、采购录单或销售回单页面。
- When 页面加载完成。
- Then 不展示底部应用切换器。
- And 不展示“返回应用中心”。

## 9. 待确认与后续建议

| 级别 | 问题 | 当前实现/推荐默认值 | 影响 |
|---|---|---|---|
| 可带假设推进 | 移除首页与切换器后，采购录单是否需要新的可见入口 | 当前仅保留 `/index.html#purchase-home` 直达地址 | 若业务用户需要在站内切换，应另行新增入口并补充权限规则 |
| 可带假设推进 | 从成员/额度页面返回业务应用的方式 | 当前使用浏览器返回或外部系统入口 | 若要求站内闭环，应补充返回目标和记忆来源页规则 |
| 可带假设推进 | 企业名称是否固定为“测试企业 1318” | 当前按原型截图固定展示 | 接入真实租户后应读取当前租户名称 |

## 10. 一致性审查结论

- 术语统一使用“销售录单”“采购录单”“成员管理”“额度管理”。
- 旧“AI 销售订单应用”“应用中心”“录单平台首页”不再作为当前入口文案。
- 默认入口、旧地址兼容、管理员菜单、管理页面和应用名称均有对应验收标准。
- 当前无阻塞研发或测试验收的 P0 问题；第 9 节三项为后续产品决策，不影响本次已实现范围。
