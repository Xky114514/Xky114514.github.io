# 采购单与采购入库单字段说明 PRD

文档版本：V0.7

更新时间：2026-07-13

适用范围：采购单提交、采购入库单待提交列表、采购入库单详情、采购单关联、确认提交和补单。

评审对象：业务负责人、产品、设计、研发和测试。

## 一、需求目标

支持正常送货和分批送货场景。采购录单系统只负责整理并提交采购单、采购入库单；单据提交至观麦后，仍需由观麦操作员完成审核。

页面信息应尽量简洁，只展示当前操作所需内容，避免使用技术状态、匹配依据或系统实现术语。

## 二、本期业务范围

### 2.1 包含场景

| 场景 | 处理方式 |
|---|---|
| 正常送货 | 一张采购单对应一张采购入库单，核对后确认提交 |
| 分批送货 | 一张采购单可以对应多张采购入库单，各批次分别核对 |
| 暂未关联采购单 | 允许先处理采购入库单，不强制关联采购单 |
| 待提交入库单补单 | 在采购入库单列表中，将录单系统内的待提交入库单补充到同一单据中处理 |
| 存在可补单入库单 | 确认提交时展示观麦待审核入库单，由操作员决定是否补单 |
| 保存 | 保存当前修改，稍后继续处理 |
| 已提交查看 | 已提交单据只读展示，不允许再次编辑或重复提交 |

### 2.2 不包含场景

- 一张采购入库单关联多张采购单。
- 多张采购单合并送货。
- 系统自动判断商品差异或自动补单。
- 采购退货、退入库等逆向业务。

## 三、统一业务口径

1. 每张采购入库单最多关联一张采购单，也可以暂不关联。
2. 采购单和采购入库单在录单系统中的最终动作统一称为“确认提交”。
3. “确认提交”只表示单据已传到观麦系统，不表示观麦已经审核完成。
4. 只有已经提交至观麦、但尚未审核的采购单，才能被采购入库单关联。
5. “补单”用于两类场景：列表中处理录单系统内的待提交采购入库单；确认提交时，将当前内容补充到观麦中已经提交、但尚未审核的采购入库单。
6. “商品数”指单据中的商品明细行数，不表示商品数量合计。
7. “识别入库数”是 AI 从原始材料中识别出的数量，仅供核对。
8. “实收数”是操作员确认的本次实际入库数量，可以修改。
9. 金额默认根据实收数和单价带出，也允许操作员直接修改；人工修改后保留人工填写结果。

## 四、采购入库单待提交列表

### 4.1 筛选与按钮

| 名称 | 页面位置与作用 |
|---|---|
| 状态 | 筛选待提交、暂存和已提交的单据 |
| 入库时间 | 按单据时间筛选 |
| 群聊 | 按来源群聊筛选 |
| 操作员 | 按当前负责人筛选 |
| 供应商 | 按供应商筛选 |
| 查询 | 位于筛选栏右侧，按当前条件刷新列表 |
| 重置 | 位于“查询”右侧，恢复默认筛选条件 |
| 刷新 | 位于列表工具栏右侧，刷新当前列表 |
| 补单 | 位于“刷新”右侧，只允许选择录单系统内尚未提交的待提交入库单进行补充处理 |

### 4.2 列表字段

| 字段 | 业务含义 | 展示要求 |
|---|---|---|
| 状态 | 当前单据的处理进度 | 展示待提交、暂存、已提交 |
| 送货情况 | 帮助识别正常送货或分批送货 | 使用简短业务描述 |
| 时间 | 当前采购入库任务的生成时间 | 不使用“系统创建时间” |
| 群聊 | 原始消息所在群聊 | 展示群名称 |
| 供应商 | 当前识别或人工填写的供应商 | 无法识别时提示补充 |
| 原文 | 本次送货的原始文字摘要 | 内容过长时省略展示 |
| 商品数 | 当前单据的商品明细行数 | 右对齐展示 |
| 关联采购单 | 当前选择的采购单 | 未选择时展示“未关联” |
| 采购单关联状态 | 采购单当前关联情况 | 只展示状态标签，不重复展示采购单号 |
| 操作员 | 当前处理人 | 待提交、暂存状态可调整 |
| 操作 | 当前可执行动作 | 已提交单据只保留“查看” |

列表不展示“提交提示”列。列表和确认提交阶段统一使用“补单”。

## 五、采购入库单详情

### 5.1 左侧信息区

左侧固定为两组页签，默认激活“群聊消息”复合页签：

| 页签 | 展示内容 |
|---|---|
| 基本信息 | 原始文件和原始消息摘要 |
| 群聊消息 | 本次任务对应的群聊上下文；右侧附加“定位来源”操作，两者共用同一激活下划线 |

“定位来源”不再作为独立的第三个页签；它以蓝色辅助操作展示在“群聊消息”右侧，用于定位原始消息在群聊中的位置和来源信息。

### 5.2 顶部基本信息

| 字段 | 展示要求 |
|---|---|
| 操作员 | 展示当前处理人 |
| 来源群聊 | 展示来源群名称 |
| 时间 | 展示任务生成时间 |
| 状态 | 标题旁展示当前状态 |

### 5.3 关联采购单

1. “关联采购单”卡片位于详情主区域最上方，在“采购入库单详情”和明细区域之前展示。
2. 该卡片中的供应商只读展示，由“AI 识别采购入库单明细”中的供应商被动同步，不提供输入框或下拉框。
3. 明细区域供应商变更后，关联采购单区域同步刷新；供应商为空时提示先填写供应商。
4. 采购单固定按当前供应商筛选。
5. 默认展示近 1 天的采购单。
6. 支持切换近 3 天、近 7 天和全部时间。
7. 每次最多选择一张采购单。
8. 已选择的采购单应持续展示，避免切换筛选时间后让用户误以为关联丢失。
9. 采购单关联状态只展示状态标签，不重复展示采购单号。
10. 只展示已经提交至观麦、但尚未审核的采购单。
11. 采购单一旦在观麦完成审核，便不可再被新增关联。
12. 已关闭或不能继续关联的采购单不可选择。

### 5.4 AI 识别采购入库单明细

| 字段 | 业务含义 | 是否可修改 |
|---|---|---|
| 序号 | 商品所在行 | 否 |
| 识别文本 | AI 识别的原始商品描述 | 否 |
| 商品名称 | 最终确认的商品名称 | 是 |
| 供应商 | 可输入下拉选项：可选择已有供应商，也可填写自定义名称；选择候选、失焦或按 Enter 后生效 | 是 |
| 入库时间 | 入库日期和起止时间，用于可补单匹配 | 是 |
| 采购数量 | 关联采购单中对应商品的采购数量；未关联时留空 | 否 |
| 实收数 | 本次实际入库数量 | 是 |
| 差异 | 采购数量减实收数；未关联时留空 | 否 |
| 识别入库数 | AI 从原始材料识别的数量和单位 | 否 |
| 单价 | 本次入库单价 | 是 |
| 金额 | 本行入库金额 | 是 |
| 备注 | 商品行补充说明 | 是 |
| SPU 名称 | 匹配到的标准商品名称 | 否 |
| 商品编码 | 匹配到的商品编码 | 否 |
| 操作 | 增加或删除商品行 | 确认前可操作 |

表格下方按钮统一命名为“新增商品”。

供应商是本页唯一可编辑的数据源，使用可输入下拉选项：下拉候选来自已有供应商，输入内容不受候选范围限制。修改后按去除首尾空格的完整名称刷新关联采购单；如果当前已经关联了其它供应商的采购单，必须先弹窗确认，确认后才解除原关联并应用新供应商。

“当前采购入库单商品数”和“关联采购单”使用单行紧凑信息条展示，字号小于明细正文；未关联时显示“未关联”，已关联时显示采购单号和商品数。

当 `|采购数量－实收数| ÷ 采购数量 > 10%` 时，对该明细行以及采购数量、实收数和差异进行高亮提示。差异提示不阻断提交。

采购入库单详情页的保存按钮统一使用“保存”，不再使用“暂存”作为按钮文案。

## 六、确认提交与补单流程

### 6.1 点击确认提交前

操作员需要完成以下核对：

- 已填写供应商。
- 已填写有效的入库日期和起止时间。
- 至少存在一条商品明细。
- 商品名称、单位和实收数完整。
- 如已关联采购单，采购单供应商与当前单一致，且采购单在观麦中仍为待审核。

信息不完整时留在详情页，并明确指出需要修改的位置。

### 6.2 不存在可补单入库单

打开普通确认弹窗，展示：

- 关联采购单及商品数；未关联时明确展示“未关联采购单”。
- 当前采购入库单商品数。
- “取消”“确认提交”按钮。

“请核对采购单与本批商品数”“本次只提交当前采购入库单”等辅助说明放入问号图标中。鼠标悬浮、键盘聚焦或点击问号时展示，不在弹窗中长期占用空间。

### 6.3 存在可补单入库单

可补单目标必须同时满足：供应商名称与当前单一致；目标单时间落在当前选择的入库日期和起止时间范围内（包含边界）；已经提交至观麦；尚未审核。弹窗标题使用“存在可补单的入库单”，页面展示：

- 可补单入库单号。
- 时间。
- 商品数。
- 行内操作：“查看详情”“补充到此单”。
- 弹窗底部操作：“取消”“确认提交”。

不展示“核对依据”、技术状态或其它解释内容。即使存在可补单记录，也必须保留“确认提交”，允许操作员不补单、直接提交当前采购入库单。

操作员可以：

- 查看候选入库单详情，核对后返回候选列表。
- 选择“补充到此单”，经二次确认后完成补单。
- 选择“确认提交”，不修改候选入库单，将当前采购入库单独立提交至观麦。
- 关闭弹窗，返回当前详情页继续修改。

### 6.4 补单二次确认

二次确认时展示当前单据和目标单据的关键信息，提醒操作员确认补单对象。若目标单据已经在观麦完成审核或不能继续修改，应停止补单并提示刷新后重新选择。

### 6.5 提交或补单成功

确认提交或补单成功后：

- 当前单据状态变为“已提交”。
- 详情页切换为只读。
- 展示本次处理结果，包括独立提交或补单结果。
- 提示“请前往观麦系统完成审核”。
- 不再提供重识别、删除、暂存或重复提交入口。

## 七、异常与提示

| 场景 | 页面处理 |
|---|---|
| 供应商为空 | 阻止确认，提示填写供应商 |
| 无商品明细 | 阻止确认，提示新增商品 |
| 商品名称、单位或实收数不完整 | 阻止确认并定位到对应行 |
| 采购单供应商不一致 | 要求更换采购单或供应商 |
| 采购单已审核或关闭 | 不允许继续关联或确认提交，提示更换或解除关联 |
| 单价异常 | 提醒操作员核对后决定是否继续 |
| 补单目标已审核或失效 | 停止补单，提示刷新候选列表 |
| 提交结果暂不明确 | 提示用户稍后刷新查看，不重复提交 |
| 两人同时处理同一单据 | 后提交者看到最新结果，并停止重复操作 |

## 八、观麦系统协同事项

本项目需要观麦提供以下业务信息和能力，具体实现方式由双方研发另行确认，不在本 PRD 中展开。

| 协同事项 | 当前项目用途 |
|---|---|
| 供应商信息 | 供应商识别、自由文本填写及采购单筛选 |
| 商品、单位和价格信息 | 商品匹配、明细核对和提交前检查 |
| 采购单及其审核状态 | 只展示观麦中尚未审核、可以被入库单关联的采购单 |
| 采购入库单及其审核状态 | 判断已提交入库单是否仍可继续补单 |
| 提交采购单 | 将操作员确认的采购单提交至观麦并等待审核 |
| 提交采购入库单 | 将操作员确认的采购入库单提交至观麦并等待审核 |
| 补单能力 | 将当前批次补充到操作员选择的观麦待审核入库单 |
| 处理结果同步 | 将成功、失败、关闭或已完成等结果反馈到当前项目 |

双方需要在上线前共同确认：

1. 观麦如何明确标识采购单“已提交但尚未审核”，以便录单系统只展示这类采购单。
2. 观麦如何明确标识采购入库单仍可补单；一旦审核完成，应立即停止补单。
3. “补充到此单”在观麦中的最终业务结果和单据展示方式。
4. 人工修改金额后，观麦是否允许直接接收该金额。
5. 近 1 天、近 3 天和近 7 天的时间范围按什么业务口径计算。
6. 供应商、商品或单位停用后，历史单据和新单据分别如何处理。
7. 提交结果不明确时，如何确认是否已经成功，避免重复入库。

## 九、验收场景

| 编号 | 场景 | 预期结果 |
|---|---|---|
| 1 | 打开待提交采购入库单 | 默认激活“群聊消息”，其右侧附加“定位来源”，两者作为同一组页签展示 |
| 2 | 查看待提交列表 | 状态列使用“状态”，时间列使用“时间”，无“提交提示”列 |
| 3 | 查看采购单关联状态 | 只显示状态标签，不重复显示采购单号 |
| 4 | 打开采购入库单详情 | “关联采购单”位于最上方，基本信息不重复展示供应商 |
| 5 | 查看供应商字段 | 供应商只在 AI 明细区域提供可输入下拉选项，关联区域只读同步展示 |
| 6 | 修改未关联单据的供应商 | 选择已有候选或输入自定义名称后，同步供应商并按完整名称刷新候选采购单 |
| 7 | 修改已关联单据的供应商 | 供应商不一致时先提示将解除原关联，确认后才应用修改 |
| 8 | 查看商品数与关联状态 | 两项在紧凑的单行信息条中展示，字号和高度小于原统计卡片 |
| 9 | 打开关联采购单区域 | 默认显示同供应商、近 1 天且观麦尚未审核的采购单，可切换其它时间 |
| 10 | 未关联采购单时查看明细 | “采购数量”和“差异”保持空白 |
| 11 | 关联采购单后查看明细 | “采购数量”取采购单对应商品数量，“差异”等于采购数量减实收数 |
| 12 | 修改实收数导致差异超过 10% | 当前行及数量差异字段实时高亮，但仍可继续提交 |
| 13 | 查看商品明细表头 | 显示“采购数量”“实收数”“差异”和“识别入库数” |
| 14 | 修改入库时间并确认提交 | 仅匹配同供应商、时间落在所选范围内、已提交至观麦且尚未审核的入库单 |
| 15 | 点击详情页保存按钮 | 按钮文案为“保存”，当前修改被保留 |
| 16 | 点击表格下方按钮 | 按钮文案为“新增商品” |
| 17 | 无可补单记录时确认提交 | 展示普通确认弹窗，弹窗中没有“暂存”按钮 |
| 18 | 存在可补单记录时确认提交 | 展示候选列表和补单操作，底部保留“确认提交”且没有“暂存”按钮 |
| 19 | 查看候选详情后返回 | 返回原候选列表，当前单据内容不变化 |
| 20 | 点击候选弹窗“确认提交” | 不执行补单，当前采购入库单独立提交至观麦 |
| 21 | 补单目标已审核 | 停止补单并提示刷新重新选择 |
| 22 | 提交或补单成功 | 状态变为已提交，详情只读，并提示前往观麦审核 |

## 十、待业务确认

1. 未关联采购单的采购入库单，是否允许所有供应商直接确认提交。
2. 单价为 0 时允许继续确认，还是必须补充后才能确认。
3. 人工金额与“实收数 × 单价”不一致时，是否需要额外提醒。
4. 补单二次确认中需要展示哪些最关键的信息。
5. 已提交后，哪些角色可以查看商品金额。
