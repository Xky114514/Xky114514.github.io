# 采购单与采购入库单字段说明 PRD

文档版本：V0.5

更新时间：2026-07-13

适用范围：采购入库单审核列表、采购入库单详情、采购单关联、确认入库和合单。

评审对象：业务负责人、产品、设计、研发和测试。

## 一、需求目标

支持正常送货和分批送货场景。操作员可以核对 AI 识别结果、选择关联采购单，并在确认入库时决定独立入库或合并到已有入库单。

页面信息应尽量简洁，只展示当前操作所需内容，避免使用技术状态、匹配依据或系统实现术语。

## 二、本期业务范围

### 2.1 包含场景

| 场景 | 处理方式 |
|---|---|
| 正常送货 | 一张采购单对应一张采购入库单，核对后确认入库 |
| 分批送货 | 一张采购单可以对应多张采购入库单，各批次分别核对 |
| 暂未关联采购单 | 允许先处理采购入库单，不强制关联采购单 |
| 存在可合单入库单 | 确认入库时展示候选列表，由操作员决定是否合单 |
| 暂存 | 保存当前修改，稍后继续处理 |
| 已确认查看 | 已确认单据只读展示，不允许再次编辑或确认 |

### 2.2 不包含场景

- 一张采购入库单关联多张采购单。
- 多张采购单合并送货。
- 系统自动判断商品差异或自动合单。
- 采购退货、退入库等逆向业务。

## 三、统一业务口径

1. 每张采购入库单最多关联一张采购单，也可以暂不关联。
2. “商品数”指单据中的商品明细行数，不表示商品数量合计。
3. “识别入库数”是 AI 从原始材料中识别出的数量，仅供核对。
4. “实收数”是操作员确认的本次实际入库数量，可以修改。
5. 金额默认根据实收数和单价带出，也允许操作员直接修改；人工修改后保留人工填写结果。
6. 是否存在可合单入库单，只在点击“确认入库”时判断。
7. 可合单列表只提供候选信息，由操作员自行决定，不自动合单。

## 四、采购入库单审核列表

### 4.1 筛选与按钮

| 名称 | 页面位置与作用 |
|---|---|
| 状态 | 筛选待确认、暂存和已确认入库的单据 |
| 入库时间 | 按单据时间筛选 |
| 群聊 | 按来源群聊筛选 |
| 操作员 | 按当前负责人筛选 |
| 供应商 | 按供应商筛选 |
| 查询 | 位于筛选栏右侧，按当前条件刷新列表 |
| 重置 | 位于“查询”右侧，恢复默认筛选条件 |
| 刷新 | 位于列表工具栏右侧，刷新当前列表 |
| 合单 | 位于“刷新”右侧，进入合单操作 |

### 4.2 列表字段

| 字段 | 业务含义 | 展示要求 |
|---|---|---|
| 状态 | 当前单据的处理进度 | 展示待确认、暂存、已确认入库 |
| 送货情况 | 帮助识别正常送货或分批送货 | 使用简短业务描述 |
| 时间 | 当前采购入库任务的生成时间 | 不使用“系统创建时间” |
| 群聊 | 原始消息所在群聊 | 展示群名称 |
| 供应商 | 当前识别或人工选择的供应商 | 无法识别时提示补充 |
| 原文 | 本次送货的原始文字摘要 | 内容过长时省略展示 |
| 商品数 | 当前单据的商品明细行数 | 右对齐展示 |
| 关联采购单 | 当前选择的采购单 | 未选择时展示“未关联” |
| 采购单关联状态 | 采购单当前关联情况 | 只展示状态标签，不重复展示采购单号 |
| 操作员 | 当前处理人 | 待确认、暂存状态可调整 |
| 操作 | 当前可执行动作 | 已确认单据只保留“查看” |

列表不展示“提交提示”列。是否存在可合单入库单，不在列表中提前提示。

## 五、采购入库单详情

### 5.1 左侧信息区

左侧固定为以下三个页签，默认展示“基本信息”：

| 页签 | 展示内容 |
|---|---|
| 基本信息 | 原始文件和原始消息摘要 |
| 群聊消息 | 本次任务对应的群聊上下文 |
| 定位来源 | 原始消息在群聊中的位置和来源信息 |

### 5.2 顶部基本信息

| 字段 | 展示要求 |
|---|---|
| 供应商 | 展示当前供应商 |
| 操作员 | 展示当前处理人 |
| 来源群聊 | 展示来源群名称 |
| 时间 | 展示任务生成时间 |
| 状态 | 标题旁展示当前状态 |

### 5.3 关联采购单

1. 采购单固定按当前供应商筛选。
2. 默认展示近 1 天的采购单。
3. 支持切换近 3 天、近 7 天和全部时间。
4. 每次最多选择一张采购单。
5. 已选择的采购单应持续展示，避免切换筛选时间后让用户误以为关联丢失。
6. 采购单关联状态只展示状态标签，不重复展示采购单号。
7. 已关闭或不能继续关联的采购单不可选择。

### 5.4 AI 识别采购入库单明细

| 字段 | 业务含义 | 是否可修改 |
|---|---|---|
| 序号 | 商品所在行 | 否 |
| 识别文本 | AI 识别的原始商品描述 | 否 |
| 商品名称 | 最终确认的商品名称 | 是 |
| 实收数 | 本次实际入库数量 | 是 |
| 识别入库数 | AI 从原始材料识别的数量和单位 | 否 |
| 单价 | 本次入库单价 | 是 |
| 金额 | 本行入库金额 | 是 |
| 备注 | 商品行补充说明 | 是 |
| SPU 名称 | 匹配到的标准商品名称 | 否 |
| 商品编码 | 匹配到的商品编码 | 否 |
| 操作 | 增加或删除商品行 | 确认前可操作 |

表格下方按钮统一命名为“新增商品”。

## 六、确认入库流程

### 6.1 点击确认入库前

操作员需要完成以下核对：

- 已选择供应商。
- 至少存在一条商品明细。
- 商品名称、单位和实收数完整。
- 如已关联采购单，采购单供应商与当前单一致，且采购单仍可使用。

信息不完整时留在详情页，并明确指出需要修改的位置。

### 6.2 不存在可合单入库单

打开普通确认弹窗，展示：

- 关联采购单及商品数；未关联时明确展示“未关联采购单”。
- 当前采购入库单商品数。
- “取消”“暂存”“确认提交”按钮。

“请核对采购单与本批商品数”“本次只处理当前采购入库单”等辅助说明放入问号图标中。鼠标悬浮、键盘聚焦或点击问号时展示，不在弹窗中长期占用空间。

### 6.3 存在可合单入库单

弹窗标题使用“存在可合单的入库单”。页面只展示：

- 可合单入库单号。
- 时间。
- 商品数。
- “查看详情”和“合并到此单”操作。

不展示“核对依据”、技术状态或其它解释内容。

操作员可以：

- 查看候选入库单详情，核对后返回候选列表。
- 选择“合并到此单”，经二次确认后完成合单。
- 关闭弹窗，返回当前详情页继续修改。

### 6.4 合单二次确认

二次确认时展示当前单据和目标单据的关键信息，提醒操作员确认合并对象。若目标单据已被其他人处理或不能继续合并，应停止合单并提示刷新后重新选择。

### 6.5 确认成功

确认或合单成功后：

- 当前单据状态变为“已确认入库”。
- 详情页切换为只读。
- 展示本次处理结果，包括独立入库或合单结果。
- 不再提供重识别、删除、暂存或重复确认入口。

## 七、异常与提示

| 场景 | 页面处理 |
|---|---|
| 供应商为空 | 阻止确认，提示选择供应商 |
| 无商品明细 | 阻止确认，提示新增商品 |
| 商品名称、单位或实收数不完整 | 阻止确认并定位到对应行 |
| 采购单供应商不一致 | 要求更换采购单或供应商 |
| 采购单已关闭 | 不允许继续确认入库 |
| 单价异常 | 提醒操作员核对后决定是否继续 |
| 合单目标已失效 | 停止合单，提示刷新候选列表 |
| 提交结果暂不明确 | 提示用户稍后刷新查看，不重复提交 |
| 两人同时处理同一单据 | 后提交者看到最新结果，并停止重复操作 |

## 八、观麦系统协同事项

本项目需要观麦提供以下业务信息和能力，具体实现方式由双方研发另行确认，不在本 PRD 中展开。

| 协同事项 | 当前项目用途 |
|---|---|
| 供应商信息 | 供应商识别、选择及采购单筛选 |
| 商品、单位和价格信息 | 商品匹配、明细核对和入库确认 |
| 采购单及其状态 | 展示可关联采购单，判断是否还能关联或确认 |
| 采购入库单及其状态 | 展示历史入库结果，判断是否可以合单 |
| 创建或更新采购单 | 将 AI 录单确认后的采购单同步到观麦 |
| 确认采购入库 | 将操作员最终确认的入库结果同步到观麦 |
| 合单能力 | 将当前批次合并到操作员选择的目标入库单 |
| 处理结果同步 | 将成功、失败、关闭或已完成等结果反馈到当前项目 |

双方需要在上线前共同确认：

1. 哪些采购单状态允许关联，哪些状态必须阻止。
2. 哪些采购入库单允许继续合单。
3. “合并到此单”在观麦中的最终业务结果。
4. 人工修改金额后，观麦是否允许直接接收该金额。
5. 近 1 天、近 3 天和近 7 天的时间范围按什么业务口径计算。
6. 供应商、商品或单位停用后，历史单据和新单据分别如何处理。
7. 提交结果不明确时，如何确认是否已经成功，避免重复入库。

## 九、验收场景

| 编号 | 场景 | 预期结果 |
|---|---|---|
| 1 | 打开待确认采购入库单 | 默认展示“基本信息”，并可切换“群聊消息”“定位来源” |
| 2 | 查看审核列表 | 状态列使用“状态”，时间列使用“时间”，无“提交提示”列 |
| 3 | 查看采购单关联状态 | 只显示状态标签，不重复显示采购单号 |
| 4 | 打开关联采购单区域 | 默认显示同供应商近 1 天采购单，可切换其它时间 |
| 5 | 修改实收数、单价或金额 | 输入框可正常修改并保留结果 |
| 6 | 查看商品明细表头 | 显示“实收数”和“识别入库数” |
| 7 | 点击表格下方按钮 | 按钮文案为“新增商品” |
| 8 | 无可合单记录时确认入库 | 展示普通确认弹窗，辅助说明收进问号 |
| 9 | 存在可合单记录时确认入库 | 只展示候选列表、详情和合单操作 |
| 10 | 查看候选详情后返回 | 返回原候选列表，当前单据内容不变化 |
| 11 | 合单目标已失效 | 停止合单并提示刷新重新选择 |
| 12 | 入库确认成功 | 状态变为已确认入库，详情只读并展示处理结果 |

## 十、待业务确认

1. 未关联采购单的采购入库单，是否允许所有供应商直接确认入库。
2. 单价为 0 时允许继续确认，还是必须补充后才能确认。
3. 人工金额与“实收数 × 单价”不一致时，是否需要额外提醒。
4. 合单二次确认中需要展示哪些最关键的信息。
5. 已确认入库后，哪些角色可以查看商品金额。
